{
  "name": "orko",
  "scripts": {},
  "env": {
    "AUTH_TOKEN": {
      "description": "2FA authentication secret. For your own good, this is required.",
      "required": true
    },
    "OKTA_BASEURL": {
      "description": "If using Okta for authentication (otherwise leave blank). The issuer from your Okta account.",
      "required": false
    },
    "OKTA_CLIENTID": {
      "description": "If using Okta for authentication (otherwise leave blank). The client id from your Okta account.",
      "required": false
    },
    "OKTA_ISSUER": {
      "description": "If using Okta for authentication (otherwise leave blank). The issue from your Okta account, with '/oauth2/default' at the end.",
      "required": false
    },
    "SIMPLE_AUTH_USERNAME": {
      "description": "If using self-hosted authentication (otherwise leave blank). A username to require for login.",
      "required": false
    },
    "SIMPLE_AUTH_PASSWORD": {
      "description": "If using self-hosted authentication (otherwise leave blank). A password to require for login.",
      "required": false
    },
    "SIMPLE_AUTH_SECRET": {
      "description": "If using self-hosted authentication (otherwise leave blank). A long, random string of characters of any type.",
      "required": false,
      "generator": "secret"
    },
    "SIMPLE_AUTH_TOKEN_EXPIRY_MINUTES": {
      "description": "If using self-hosted authentication (otherwise leave blank). The time before each token issued will expire and the user will be forced to log in again, in minutes.",
      "required": false,
      "value": "1440"
    },
    "JAVA_OPTS": {
      "description": "You generally shouldn't need to change any of this. It's been well tuned for a Hobby Heroku instance.",
      "value": "-server -Xmx185m -Xms185m -Xss256k -XX:MaxMetaspaceSize=80m -XX:+UseG1GC -Dsun.net.inetaddr.ttl=60 -Dio.netty.leakDetectionLevel=advanced"
    },
    "MAVEN_CUSTOM_GOALS": {
      "description": "Used by the Heroku buildpack. Do not change.",
      "value": "clean package"
    },
    "MAVEN_CUSTOM_OPTS": {
      "description": "Used by the Heroku buildpack. Do not change.",
      "value": "-Pproduction --update-snapshots -DskipTests=true -T 1C"
    },
    "TELEGRAM_BOT_TOKEN": {
      "description": "The Telegram bot API token. Can be omitted, in which case Telegram notifications won't be used.",
      "required": false
    },
    "TELEGRAM_CHAT_ID": {
      "description": "The Telegram chat ID. May be omitted if TELEGRAM_BOT_TOKEN is.",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "addons": ["mongolab", "papertrail"],
  "buildpacks": [
    {
      "url": "heroku/java"
    }
  ]
}
